import React from "react"
import { connect } from "react-redux"

const Checkout = () => {
    return <><p>Checkout Here</p></>
}

const mapStateToProps = (state) => ({

})

export default connect(mapStateToProps)(Checkout)
